# --- Core Dependencies ---
PyYAML==6.0.3
rich # added by audit
numpy  # app.py (np arrays for radar/scatter charts)
pandas # added by audit
Pillow # added by audit
python-dotenv==1.2.2

# --- Model Backends & ML ---
ultralytics  # YOLOv8 + ByteTrack for offline tracking pre-computation
mistralai==2.1.0
openai # added by audit
torch # added by audit
transformers # added by audit
bitsandbytes  # 4-bit / 8-bit quantisation pour les modèles locaux trop grands pour le GPU
qwen-vl-utils==0.0.14
molmo-utils==0.0.1

# --- Analysis Interface ---
streamlit
plotly
statsmodels

# --- Utilities ---
Pygments==2.19.2
charset-normalizer==3.4.6
cuda-pathfinder==1.4.3
einops==0.8.2 # possibly unused — verify before removing
eval_type_backport==0.3.1
mdurl==0.1.2
pip-chill==1.0.5
pydantic_core==2.41.5
