{tracking_context}

You are analyzing N frames from a driving scene captured by an ego vehicle.
The frames are provided in chronological order, numbered from 1 to N.

Your task is to extract structured semantic information for EACH frame independently.

Return ONLY a valid JSON object. No explanation, no markdown, no preamble.

CRITICAL — NUMBER OF KEYS:
You MUST return exactly as many frame keys as there are images in this message.
If you receive 2 images → your JSON must have exactly "frame_1" and "frame_2".
If you receive 3 images → your JSON must have exactly "frame_1", "frame_2" and "frame_3".
If you receive 4 images → your JSON must have exactly "frame_1", "frame_2", "frame_3" and "frame_4".
Returning fewer keys than images received is an error. Every image must have its own key.

The JSON must follow this exact schema — one entry per frame received:

{
  "frame_1": {
    "scene_context": {
      "location": "<one of: road, sidewalk, crosswalk, intersection, parking, other>",
      "description": "<one sentence describing the overall scene>"
    },
    "pedestrians": [
      {
        "track_id": <integer assigned by the tracking system, or -1 if not detected by tracker>,
        "atomic_action": "<one of: walking, running, standing, sitting, bending, squatting, laying down, jumping, none of the above>",
        "simple_context": "<one of: walking along the side of the road, walking on the road, waiting to cross street, crossing a street at pedestrian crossing, jaywalking (illegally crossing NOT at pedestrian crossing), entering a building, exiting a building, biking, motorcycling, none of the above>",
        "communicative": "<one of: none of the above, looking into phone, talking on phone, talking in group>",
        "transporting": "<one of: none of the above, carrying with both hands, pushing, pulling>",
        "age": "<one of: child, adult, senior over 65>",
        "spatial_relations": ["<relation to another entity or scene element>"]
      }
    ],
    "vehicles": [
      {
        "track_id": <integer assigned by the tracking system, or -1 if not detected by tracker>,
        "type": "<one of: car, truck, bus, motorcycle, bicycle, other>",
        "motion_status": "<one of: stopped, moving, parked>",
        "trunk_open": "<one of: open, closed>",
        "doors_open": "<one of: open, closed>",
        "spatial_relations": ["<relation to another entity or scene element>"]
      }
    ]
  },
  "frame_2": { "...same structure..." },
  "frame_N": { "...same structure..." }
}

Rules:
- Produce exactly one "frame_K" key per image received, from frame_1 to frame_N. No more, no less.
- Describe each frame based only on what is visible in that specific frame.
- If no pedestrians are visible in a frame, set "pedestrians" to [].
- If no vehicles are visible in a frame, set "vehicles" to [].
- Use only the allowed values listed for each field. Do not invent new categories.
- For track_id: match each detected entity to the tracking context above using bbox proximity and appearance. Use -1 only if no tracker entry plausibly corresponds to the entity.

FINAL CHECKLIST — verify silently before outputting:
1. FRAME COUNT: count your images → your JSON must have exactly that many frame keys.
2. TRACK_ID ASSIGNMENT: every pedestrian and vehicle must have a track_id (integer or -1).
3. ENUM COMPLIANCE: every field value must be from the allowed list above.
